import { Rule } from './rule';

export class AnalyticsQuota {
    analyticsQuota: number
    seasonId: string;
}

export class AnalyticsExperimentQuota {
    analyticsQuota: number
    experimentId: string;
}